1. 定义一个数组 [1,2,3,4,5]，然后用for循环在控制台遍历打印出数字里面的元素
2. 定义一个学生对象，学生对象包括：学号(1001)，姓名('张三')，成绩(89.9)。然后修改名字为'李四'，
并在控制台打印这个对象的学号，姓名，成绩
3. 定义一个学生对象数组，这个数组里面包括3个学生(张三，李四，王五)，学号成绩随便给。然后写一个函数，
这个函数用来求数组里面的学生成绩的平均值。通过调用这个函数，在控制台打印平均值。